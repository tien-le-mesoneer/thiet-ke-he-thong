---
plan_week: 1
elapsed_week: 7
phase: 1
active_impl: node
started: 2026-07-12
exercises_done: [url-shortener]
weak_concepts: [capacity-estimation, right-sizing, percentile-definitions, queueing-vs-processing-time]
---
## Log
- 2026-07-12: Progress file created. Study plan started.
- 2026-07-13 Week 1 (reading): reliability quiz (3 Qs) via notebook. Q2 (human/software faults) flagged not-fully-understood → tutored (correlated vs independent failures; redundancy only beats independent faults). Added weak_concept: human-and-software-faults.
- 2026-07-13 Week 1 (reading): full reliability explanation via ask mode; user confirmed solid → cleared weak_concept human-and-software-faults. Saved reliability summary to notebook as a note.
- 2026-07-13 Week 1 (build): W1.migrations GREEN — reused local Postgres (created role app + db deliveroo), migration applied all 4 module schemas. Fixed esbuild platform-binary mismatch (reinstalled node_modules) which also unblocks npm dev/test. Week 1 build fully green.
- 2026-07-15 Week 1 (exercise): URL shortener attempt graded (avg ~B). Strengths: structure, bottleneck spotting, obfuscated-id instinct. Fixes: storage units 1000× off, NFR-vs-estimate mismatch, 301→302, right-size DB (single Postgres, not NoSQL). exercises_done += url-shortener; weak: capacity-estimation, right-sizing. User wants to build it as a real service in the practice project (next: brainstorm apps/url-shortener).
- 2026-08-24 Week 1 (reading): taught percentiles & tail latency (DDIA ch.1 "Describing Performance", pp. 13-16) with per-topic source refs. Quiz Q2 re-posed but still UNANSWERED; Q3 never written. weak: tail-latency-amplification.
- 2026-08-24 (bookkeeping): elapsed_week corrected 1 -> 7 (started 2026-07-12, 6 weeks wall-clock). plan_week stays 1; slippage = +6.
- 2026-08-24 (pivot): user moved focus to monitoring/observability (syllabus Week 14). Running it as a PARALLEL TRACK against apps/url-shortener-node rather than bumping plan_week, because the Week-14 build assumes the deliveroo saga chain (W2.enforced still RED). Resolved the 2 blocked decisions in the OTel spec: (1) full OTel SDK -> OTLP -> Collector migration, dropping prom-client /metrics; (2) first SLO = p99 latency (99% of redirects < 50ms/30d), availability demoted to a panel. Spec updated.
- 2026-08-24 Week 14 (parallel track, slice 1/5): observability stack GREEN. New infra/observability/ (OTel Collector 0.121.0 + Prometheus 3.2.1 + Grafana 11.5.2, podman compose). url-shortener src/tracing.ts switched ConsoleSpanExporter -> OTLP; dev:otel/start:otel now set OTEL_SERVICE_NAME. Verified end-to-end: POST /api/v1/urls + redirect (302) produced HTTP + mongodb spans with service.name=url-shortener; otelcol_receiver_accepted_spans_total=14 == otelcol_exporter_sent_spans_total=14 (zero dropped). App suite 23/23 green with OTEL unset (zero-overhead default holds). Next: slice 2 (Tempo).
- 2026-08-24 Week 14 (parallel track): k6 confirmed as the load driver for the monitoring work (k6 v1.6.1 + existing load/redirect.js, whose p(99)<50 threshold already matches the chosen SLO). Ran 980,125 reqs @ 8,166 req/s: p99 = 41.65ms (PASS, but 83% of budget vs the 18.06ms baseline -> queueing under load, DDIA ch.1 'Describing Performance' live). Two findings recorded in the spec: (1) thin SLO headroom at peak; (2) 26.6% of spans dropped SILENTLY app-side in BatchSpanProcessor (collector refused=0, send_failed=0) -> SLI must come from metrics/histograms, not traces; fix is sampling, not a bigger queue. Distribution mean 11.4 / p50 9.92 / p95 25.94 / p99 41.65 / max 145.19 ms is a textbook mean-vs-percentile illustration.
- 2026-08-24 Week 14 (parallel track): moved the whole observability stack to a dedicated 7xxx host-port block (Grafana 7080, Prometheus 7090, Tempo 7200 reserved, OTLP 7317/7318, health 7133, app-metrics 7889), each keeping its upstream default's digits; container-internal ports untouched so upstream configs stay standard. Overridable via infra/observability/.env. Removed the superseded prometheus/grafana pair from the app compose (they collided on 9090/3002). Re-verified end-to-end on the new ports: 39 spans from 25 redirects, collector logs and Prometheus agree. Two gotchas hit and documented: a stale app process on :3001 (EADDRINUSE) silently served requests while exporting to the dead old endpoint, and bare sum() over a counter double-counts a restarted collector for ~5min (use increase()).
- 2026-08-24 Week 14 (parallel track, slice 3/5 DONE, taken before slice 2): migrated url-shortener from prom-client to OTel metrics over OTLP. src/tracing.ts -> src/otel.ts (now bootstraps traces AND metrics); latency histogram http_request_duration_seconds with explicit buckets incl. an exact 0.05s boundary via OTel View; counters ported to .add(1); /metrics route + prom-client removed. Tests 26/26 (3 new: bucket contains SLO threshold, buckets ascend/straddle, instruments are safe no-ops). FIRST REAL SLI: 99.8943% over 322,924 reqs @ 8,070 req/s -> PASS, 10.6% of error budget used. Key lesson measured: counted SLI (exact) vs histogram_quantile p99 20.90ms vs k6 client-side 15.74ms = 33% interpolation error -> never compute an error budget by interpolation. Also fixed a cardinality bomb I shipped in slice 1: resource_to_telemetry_conversion=true put 17 labels on every series (process_pid, full argv); now disabled, 1 label (job).
- 2026-08-24 (BUG FOUND, not fixed): a k6 run failed with p99=66.97ms and 0 cache hits. Root cause was NOT latency - k6 setup() got a 500 (E11000 dup key on short_code), so data.code was undefined and all 112k requests hit /undefined -> 404s, a different code path. Underlying issue is test isolation: test/idrange.test.ts deletes the counters collection but not links, while repo/clicks tests delete links but not counters, so `npm test` can leave the shared dev DB with a reset id counter and surviving links -> the allocator re-issues ids whose short_codes already exist. Tests share the dev database. Clean re-run passed: 100% checks, p99 15.74ms.
- 2026-08-24 (BUG FIXED): test isolation. Tests now run against dedicated throwaway stores (shorturl_test + redis db 1) set by the npm test script, with test/setup.ts preloaded via --import as a hard guard that REFUSES to start unless MONGO_URL ends in _test and REDIS_URL names a non-zero redis db. Verified all three branches (dev db refused, redis db0 refused, correct targets allowed) and proved the dev database is untouched by a full run (links=3 counterSeq=1000 before AND after). Also made counters/links cleanup consistent in idrange/repo/clicks tests (they are coupled: short codes derive from the counter), and hardened load/redirect.js so setup() throws instead of proceeding with an undefined code. Tests 26/26.
- 2026-08-24 (CORRECTION): my earlier '33% interpolation error' claim was invalid - it compared k6 and histogram_quantile over DIFFERENT time windows. On matched data (364,240 reqs, k6 p99=22.47ms) histogram_quantile gives 22.82ms = 1.6% error. The real argument for counting is bucket WIDTH: with the 0.05 boundary the SLI is exact (99.8268%, 17.3% of budget); remove it and the enclosing 0.025-0.1 bucket makes interpolation report 99.5633% = 43.7% of budget - 2.5x the burn from the same data. Always compare metrics over the same window before drawing a conclusion.
- 2026-08-24 Week 14 (parallel track, slice 4/5 DONE): SLO alerting. prometheus/rules.yml records the error ratio at 5m/30m/1h/6h/3d and defines multi-window multi-burn-rate alerts (14.4x critical 1h+5m, 6x warning 6h+30m, 1x ticket 3d+6h) + an absent() guard. New Grafana dashboard 'SLO - Redirect Latency' (7 panels: SLI, budget remaining gauge, burn rate, p99 marked explicitly as dashboard-only, burn-by-window with threshold lines, good-vs-total, slow-per-second). Verified live: 11 rules loaded healthy, all 4 alerts inactive under healthy load (burn 0.01x over 531,183 reqs @ 9,654 req/s, p99 20.9ms). Verified logic via promtool test rules - 4 cases incl. negatives (healthy fires nothing; 8x fires high but NOT critical). Key concept learned: multi-window = long window asks 'is it real', short window asks 'is it still happening' so the alert clears on recovery instead of smouldering. Documented blind spot: latency SLI filtered to status=302 goes ABSENT not BAD in a total outage -> needs a separate availability SLO (not yet written).
- 2026-08-25 Week 14 (parallel track, slice 2/5 DONE - all 5 slices now built except the game-day): Tempo 2.7.1 added (host 7200, local storage, 24h retention, max_block_duration 1m so traces are searchable seconds after they end). Collector trace pipeline switched debug -> otlp/tempo. Grafana Tempo datasource provisioned with tracesToMetrics + serviceMap + nodeGraph; Prometheus gained --web.enable-remote-write-receiver for Tempo's span-derived RED metrics. Verified: Tempo /ready 200, service.name=url-shortener searchable, and a POST trace showing real parent/child nesting (POST 15.34ms -> mongodb.insert 7.43ms). MAJOR CORRECTION: the 26.6% span loss blamed on BatchSpanProcessor in slice 1 was actually caused by the debug exporter - same SDK/queue, traces to Tempo instead, gave 471,177 spans for 471,176 reqs at 10,466 req/s = ZERO loss at HIGHER throughput. debug verbosity:detailed formats every span to stdout, throttling the collector into backpressure. Lesson: a debugging aid can be the bottleneck it appears to diagnose; always re-measure after changing the pipeline. Sampling still wired (npm run load:otel, 9.93% measured) but for COST not correctness. Gotcha found: tsx watch does NOT propagate sampler env to the child it respawns - dev:otel always runs 100%, hence the separate non-watch load:otel script.
- 2026-08-25 Week 1 (reading): scalability quiz COMPLETED (Q1 correct; Q2 1.5/3; Q3 2.5/3). Also taught the mitigation half of tail latency, which we had never covered: reduce N, hedged/backup requests (Dean & Barroso 'The Tail at Scale' via DDIA ch.1), deadline propagation (Building Microservices ch.12 Resiliency/Time-Outs), bulkheads/circuit breakers, partial results. CLEARED weak_concept tail-latency-amplification - the amplification arithmetic was solid unprompted (1-0.99^10 = 9.56%, and correctly spotted 0.99^20 => 18.2% breach). NEW weak_concepts: percentile-definitions (read p50=20ms as '99% take 20ms' in Q2a; also did not solve q^20=0.99 -> each service needs p99.95) and queueing-vs-processing-time (in Q3b reached for network/client-render before queueing delay; also missed survivorship bias - server logs only contain requests that were actually served). Noted that url-shortener has fan-out=1, so it has measured tail LATENCY but never amplification; that arrives with the deliveroo saga chain.
- 2026-08-25 Week 1 (build): W1.modules GREEN. All four modules now have both routes.ts and service.ts. Extracted users + catalog service layers from their routes (SQL moved out of the route handlers, following the orders/ convention: service owns logic and knows nothing about Fastify, routes stay thin and only map to status codes). Added payments/routes.ts as READ-ONLY (GET /payments/order/:orderId) - chargePayment stays internal because it takes the order transaction handle and an HTTP endpoint that charges an arbitrary order id would be a hole, not a feature. Verified live against Postgres, not just typecheck: user create/get/404, restaurant + menu item create/list, order placed, and the new payments route returning amount_cents=19000 = 2 x 9500 (correct). Week 1 non-self concepts now ALL GREEN (health, config, logging, modules, migrations, build). NOTE npm test still fails with "Could not find 'test/'" - that is the pre-existing W2.tests-exist RED (0 files ever tracked in git), not a regression.
- 2026-08-30 (infra): merged the three compose files into one root compose.yaml (app services stay on the HOST; only datastores + telemetry backends are containerised) and added measured mem/cpu limits to all 7 containers, env-overridable. Capping is dramatic because these services grow to fill whatever they are given: total idle went 1350MB -> 241MB at a 100m cap with everything still functional. Node apps capped via NODE_OPTIONS --max-old-space-size, default 100 (NODE_MAX_HEAP_MB overrides). Measured the app floor rather than guessing: 100MB = 13,655 req/s p99 7.02ms OK; 75MB = 11,870 req/s OK, RSS 145MB; 50MB = FATAL 'Reached heap limit' at 921ms, so the floor is between 50 and 75 and 100 keeps margin. IMPORTANT correction found by testing: mongo at 100m looked fine at idle (74MB) but was OOM-KILLED by the test suite - WiredTiger sizes its cache from HOST RAM and ignores the container limit, so it needs mem_limit 512m AND an explicit --wiredTigerCacheSizeGB 0.25; raising the limit alone only moves the cliff. Lesson: idle memory is not a requirement, and a container limit is not visible to the process inside unless you tell it. W1.config in acceptance.node.md now points at the root compose. Both suites green after the change (url-shortener 26/26, deliveroo 8/8).
- 2026-08-30 (infra): reverted the single root compose back to three per-area files, and CONTAINERISED both apps so memory is genuinely enforceable (a host process gets no cgroup, so NODE_OPTIONS alone bounds only the V8 heap, not RSS). New Dockerfiles with dev/build/prod targets. Apps join an external shared network sd-net so they can reach otel-collector across compose projects. Two ceilings now, and both are needed: mem_limit 256m bounds RSS, NODE_MAX_HEAP_MB 100 bounds the heap inside it (RSS ~1.8x heap). Verified containerised under load: 8,190 req/s, p99 9.89ms, 155.8MB of 256MB, oomkilled=false. Container-to-container OTLP confirmed (46 spans, app resolves otel-collector by name). Gotchas hit: (1) podman-compose refuses to synthesise the implicit `default` network alongside an explicit one - every service is on sd-net only; (2) tsx watch does not pass externally-set env to the child it respawns, which would silently drop the compose-provided OTLP endpoint, so containers run a watch-free `container:otel` script and hot reload stays host-only; (3) OTEL_EXPORTER_OTLP_ENDPOINT had to become overridable (${VAR:-default}) since containers use otel-collector:4318 while the host uses localhost:7318; (4) deliveroo postgres moved to host port 5433 because Homebrew postgres shadows 5432. Also saved a capacity-estimation note: memory does not scale with req/day - at 10k/day Little's Law gives 0.008 requests in flight, so the per-request term is 250 BYTES and the ~420MB total is entirely fixed cost, identical at 10M/day.
- 2026-08-30 Week 14 (parallel track, slice 5/5 DONE - observability build complete): game-day. Killed Redis under load. HYPOTHESIS WAS WRONG: reads did not degrade, the service DIED. Three real bugs found, all in the cache-failure path. (1) `void flushOnce()` in the click flusher discarded the promise, so a Redis failure in a background timer became an unhandled rejection and killed the process - the request path was defended, the timer was not. (2) No timeout on cache calls; took three attempts - enableOfflineQueue:false breaks startup (all 26 tests failed), maxRetriesPerRequest:1 breaks reconnects (9 failed), and the right answer was an application-level withTimeout(100ms) because the cache is an optimisation and an optimisation is never worth waiting on. Subtlety: when the timeout wins the race, the original promise's later rejection is unhandled unless you p.catch(()=>{}) it. (3) Unguarded cacheSet in shorten() turned every WRITE into a 500 while reads were fine. WORST FINDING - the observability stack saw NONE of it: all 4 alerts inactive, SLO error ratio 0, dashboard green while the service was dead, because the latency SLI filters status=302 and goes ABSENT not BAD in a total outage; RedirectLatencySLIMissing needs ~15min (5m rate window + for:10m) to notice. Confirms the availability-SLO gap is real and must be closed. Final state verified: redis dead -> read 302 in 322ms, write 201 in 111ms, app running, tests 26/26. Process lessons: podman compose up -d --build reuses the CONTAINER so two experiments ran stale code; always verify with podman exec grep.
